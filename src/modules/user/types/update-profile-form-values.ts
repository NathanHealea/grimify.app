/**
 * Form values submitted by the {@link EditProfileForm} component.
 *
 * Both fields map directly to columns on the `profiles` table.
 */
export type UpdateProfileFormValues = {
  display_name: string
  bio: string
}
