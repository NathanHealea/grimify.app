/**
 * Default values used to pre-populate the profile setup form.
 *
 * Mirrors the form's field names so values can be spread into inputs.
 */
export type ProfileFormValues = {
  display_name: string
}
